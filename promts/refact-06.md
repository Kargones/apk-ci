Внимательно изучи код библиотеки из каталога tmp.
Сравни количество передаваемых в при запуске rac параметров с количеством параметров в реализации этих функций в данном проекте.
Обеспечь правильное количество и корректный состав во всех функциях вызывающий утилиту rac.
Скорректируй функцию логгеза при выводе сообщений от rac.
В данный момент вывод выглядит следующим образом:
2025/08/06 09:11:15 WARN RAC command failed, retrying attempt=1 error="RAC command failed: exit status 0xffffffff, output: \x8e訡\xaa\xa0 ࠧ\xa1\xae\xe0\xa0 \xaf\xa0ࠬ\xa5\xe2\xe0\xa0: --cluster-user=gitops\r\n"
что говорит о том что rac использует для вывода кодировку CP866.
Требуется скорректировать функцию логгера, чтобы она корректно отображала сообщения от rac.
В функции логгера необходимо добавить преобразование сообщения от rac из CP866 в UTF-8.

Для проверки корректности работы запусти на выполнение тест TestManageServiceMode из файла servicemode_test.go.
Тест должен пройти успешно.

