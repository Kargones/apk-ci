Создай файл Test all action.yaml в корменом каталоге по анлогии с файлом DbUpdate.yaml.

Запиши в него шаки для тестирования всех доступных действий.
каждый шаг должен включаться отдельным input с именем action_<номер_шага> типа булево.
значения по умолчанию для всех input должны быть false.
каждому шагу должен быть присвоен уникальный номер.
номер шага должен быть от 1 до 10.
каждый шаг должен быть описан в комментарии перед ним.
комментарий должен начинаться с #.
комментарий должен быть на одной строке с шагом.
если действие требует передачи параметра добавь в input параметр с именем action_<номер_шага>_<имя_параметра> и передавай его в действие.
Добавь необходимые параметры в action.yaml и передавай их в действие.
Доработай парсер входных параметров чтобы учесть эти изменения.



