// Тестовый BSL файл с синтаксическими ошибками для проверки логики перезапуска

Функция ПроблемнаяФункция()
    // Отсутствует объявление переменных
    Результат = "Тест";
    
    // Неправильный синтаксис условия
    Если Результат = "Тест" Тогда
        // Отсутствует КонецЕсли
        Сообщить("Проблемный код");
    
    // Неправильное завершение функции
    Возврат Результат;
    // Отсутствует КонецФункции

Процедура НеправильнаяПроцедура()
    // Неправильное использование переменных
    НеОбъявленнаяПеременная = 123;
    
    // Синтаксическая ошибка в цикле
    Для Счетчик = 1 По 10 Цикл
        Сообщить(Счетчик);
    // Отсутствует КонецЦикла
    
// Отсутствует КонецПроцедуры

// Неправильная структура модуля
Переменная БезОбъявления;