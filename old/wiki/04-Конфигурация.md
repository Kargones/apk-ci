# Руководство по конфигурации и настройке

Данный документ содержит полное руководство по настройке и конфигурированию apk-ci для различных сред и сценариев использования.

## Общие принципы конфигурации

### Иерархия источников конфигурации

apk-ci использует иерархическую систему конфигурации с следующим приоритетом:

1. **Переменные окружения** (наивысший приоритет)
2. **Конфигурационные файлы** (средний приоритет)
3. **Значения по умолчанию** (наименьший приоритет)

### Структура конфигурационных файлов

```
project-root/
├── app.yaml              # Основные настройки приложения
├── project.yaml          # Настройки проекта и репозитория  
├── dbconfig.yaml         # Конфигурация баз данных
├── sec.config.yaml       # Секретные данные (с SOPS)
└── service-mode-config-*.yaml  # Конфигурации сервисного режима
```

## Основные конфигурационные файлы

### app.yaml - Настройки приложения

Содержит основные настройки приложения, пути к исполняемым файлам и параметры подключений.

```yaml
# Общие настройки
logLevel: "Debug"          # Уровень логирования: Debug, Info, Warn, Error
workDir: "/tmp/benadis"    # Рабочая директория
tmpDir: "/tmp/benadis/temp" # Временная директория
timeout: 30                # Общий таймаут операций в секундах

# Пути к исполняемым файлам 1С
paths:
  bin1cv8: "/opt/1cv8/x86_64/8.3.27.1606/1cv8"          # Путь к 1cv8
  binIbcmd: "/opt/1cv8/x86_64/8.3.27.1606/ibcmd"        # Путь к ibcmd
  edtCli: "/opt/1C/1CE/components/1c-edt-2024.2.6+7-x86_64/1cedtcli"  # Путь к EDT CLI
  rac: "/opt/1cv8/x86_64/8.3.27.1606/rac"               # Путь к RAC

# Настройки RAC (Remote Administration Console)
rac:
  port: 1545      # Порт RAC сервера
  timeout: 30     # Таймаут операций RAC в секундах
  retries: 3      # Количество повторных попыток

# Пользователи по умолчанию
users:
  rac: "admin"           # Пользователь кластера RAC
  db: "db_user"          # Пользователь информационной базы
  mssql: "mssql_user"    # Пользователь MS SQL Server
  storeAdmin: "store_admin"  # Администратор хранилища

# Настройки восстановления БД
dbrestore:
  database: "master"     # База данных по умолчанию для подключения
  timeout: "300"         # Таймаут восстановления в секундах
  autotimeout: true      # Автоматический расчет таймаута
```

### project.yaml - Настройки проекта

Содержит специфичные для проекта настройки, включая информацию о репозитории и расширениях.

```yaml
# Основная информация о проекте
projectName: "MyERPSystem"           # Имя проекта
storeDb: "local"                     # База данных хранилища: "local" или имя из dbconfig.yaml

# Массив расширений проекта
addArray:
  - "CustomReports"                  # Расширение пользовательских отчетов
  - "IntegrationModule"              # Модуль интеграции
  - "SecurityEnhancement"            # Расширение безопасности

# Настройки Git репозитория (опционально)
gitSettings:
  defaultBranch: "main"              # Основная ветка
  developBranch: "develop"           # Ветка разработки
  
# Настройки среды
environment:
  type: "development"                # Тип среды: development, testing, production
  region: "ru"                       # Регион
```

### dbconfig.yaml - Конфигурация баз данных

Содержит информацию о доступных базах данных и серверах.

```yaml
# Массив доступных баз данных
databases:
  # Тестовая база данных
  - name: "TestDB"                   # Имя информационной базы
    server: "test-sql.company.ru"    # Сервер MS SQL
    oneServer: "test-1c.company.ru"  # Сервер 1С:Предприятие
    database: "TestDatabase"         # Имя базы данных в MS SQL
    description: "Тестовая среда"    # Описание
    
  # Продуктивная база данных  
  - name: "ProductionDB"
    server: "prod-sql.company.ru"
    oneServer: "prod-1c.company.ru"
    database: "ProductionDatabase"
    description: "Продуктивная среда"
    
  # База данных для разработки
  - name: "DevDB"
    server: "dev-sql.company.ru"
    oneServer: "dev-1c.company.ru"
    database: "DevelopmentDatabase"
    description: "Среда разработки"
    
  # Локальная файловая база (для тестирования)
  - name: "LocalFileDB"
    server: ""                       # Пустой для файловой базы
    oneServer: ""                    # Пустой для файловой базы
    database: "/opt/1c/databases/local"  # Путь к файловой базе
    description: "Локальная файловая база"
```

### sec.config.yaml - Секретные данные

Содержит пароли и токены доступа. Рекомендуется шифрование с помощью SOPS.

```yaml
# Пароли пользователей
passwords:
  db: "password123"                  # Пароль пользователя ИБ
  mssql: "sqlpassword456"            # Пароль MS SQL
  storeAdminPassword: "storepass789" # Пароль администратора хранилища
  racPassword: "racpass321"          # Пароль RAC кластера

# Токены доступа к внешним системам
tokens:
  giteaToken: "gitea_token_here"     # Токен доступа к Gitea
  
# Настройки HASP
hasp:
  serverAddress: "10.50.69.155"     # Адрес сервера HASP
  port: 475                         # Порт сервера HASP
```

#### Шифрование с помощью SOPS

Для безопасного хранения секретов используется SOPS:

```bash
# Шифрование файла
sops -e sec.config.yaml > sec.config.enc.yaml

# Расшифровка файла
sops -d sec.config.enc.yaml > sec.config.yaml

# Редактирование зашифрованного файла
sops sec.config.enc.yaml
```

## Переменные окружения

### Базовые переменные

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BR_COMMAND` | Выполняемая команда | `git2store` |
| `BR_LOG_LEVEL` | Уровень логирования | `Debug` |
| `BR_WORK_DIR` | Рабочая директория | `/tmp/benadis` |
| `BR_TMP_DIR` | Временная директория | `/tmp/benadis/temp` |

### Переменные проекта и репозитория

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BR_OWNER` | Владелец репозитория | `mycompany` |
| `BR_REPO` | Имя репозитория | `erp-config` |
| `BR_PROJECT_NAME` | Имя проекта | `MyERPSystem` |
| `BR_INFOBASE_NAME` | Имя информационной базы | `TestDB` |

### Переменные Gitea интеграции

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BR_ACCESS_TOKEN` | Токен доступа к Gitea | `gitea_token_123` |
| `BR_ISSUE_NUMBER` | Номер обрабатываемой задачи | `456` |
| `GITEA_URL` | URL сервера Gitea | `https://git.company.ru` |

### Переменные RAC

| Переменная | Описание | Пример |
|------------|----------|--------|
| `RAC_PATH` | Путь к исполняемому файлу RAC | `/opt/1cv8/.../rac` |
| `RAC_SERVER` | Сервер 1С | `localhost` |
| `RAC_PORT` | Порт RAC | `1545` |
| `RAC_USER` | Пользователь кластера | `admin` |
| `RAC_PASSWORD` | Пароль кластера | `password` |
| `RAC_TIMEOUT` | Таймаут RAC операций | `30` |

### Переменные сервисного режима

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BR_TERMINATE_SESSIONS` | Завершить активные сессии | `true` |
| `BR_SERVICE_MODE_MESSAGE` | Сообщение блокировки | `Техническое обслуживание` |

### Переменные баз данных

| Переменная | Описание | Пример |
|------------|----------|--------|
| `MSSQL_PASSWORD` | Пароль MS SQL Server | `sql_password` |
| `DB_TIMEOUT` | Таймаут операций с БД | `300` |

## Конфигурации для разных сред

### Development (Разработка)

**app.yaml:**
```yaml
logLevel: "Debug"
workDir: "/tmp/benadis-dev"
tmpDir: "/tmp/benadis-dev/temp"
timeout: 60

paths:
  bin1cv8: "/opt/1cv8/x86_64/8.3.27.1606/1cv8"
  binIbcmd: "/opt/1cv8/x86_64/8.3.27.1606/ibcmd"
  edtCli: "/opt/1C/1CE/components/1c-edt-2024.2.6+7-x86_64/1cedtcli"
  rac: "/opt/1cv8/x86_64/8.3.27.1606/rac"

rac:
  port: 1545
  timeout: 60
  retries: 5
```

**Переменные окружения:**
```bash
export BR_LOG_LEVEL="Debug"
export RAC_SERVER="dev-1c.company.ru"
export BR_INFOBASE_NAME="DevDB"
```

### Testing (Тестирование)

**app.yaml:**
```yaml
logLevel: "Info"
workDir: "/opt/benadis/test"
tmpDir: "/opt/benadis/test/temp"
timeout: 45

rac:
  port: 1545
  timeout: 45
  retries: 3
```

**Переменные окружения:**
```bash
export BR_LOG_LEVEL="Info"
export RAC_SERVER="test-1c.company.ru"
export BR_INFOBASE_NAME="TestDB"
```

### Production (Продуктив)

**app.yaml:**
```yaml
logLevel: "Warn"
workDir: "/opt/benadis/prod"
tmpDir: "/opt/benadis/prod/temp"
timeout: 30

rac:
  port: 1545
  timeout: 30
  retries: 2
```

**Переменные окружения:**
```bash
export BR_LOG_LEVEL="Warn"
export RAC_SERVER="prod-1c.company.ru"
export BR_INFOBASE_NAME="ProductionDB"
```

## Специальные конфигурации

### Конфигурация сервисного режима

Файлы `service-mode-config-*.yaml` для различных информационных баз:

**service-mode-config-DEV-16-AS-003.yaml:**
```yaml
# Конфигурация сервисного режима для базы DEV-16-AS-003
infobaseName: "DEV-16-AS-003"
racServer: "dev-1c-cluster.company.ru"
racPort: 1545
racUser: "cluster_admin"
clusterName: "dev-cluster"
defaultMessage: "Техническое обслуживание системы"
terminateSessionsTimeout: 60
maxWaitTime: 300
```

**service-mode-config-PROD-01.yaml:**
```yaml
# Конфигурация сервисного режима для продуктивной базы
infobaseName: "PROD-ERP-01"
racServer: "prod-1c-cluster.company.ru"
racPort: 1545
racUser: "cluster_admin"
clusterName: "production-cluster"
defaultMessage: "Плановые технические работы"
terminateSessionsTimeout: 120
maxWaitTime: 600
enableNotifications: true
notificationChannels:
  - email
  - slack
```

## Настройка путей к исполняемым файлам

### Linux

Стандартные пути установки на Linux:

```yaml
paths:
  # Платформа 1С версии 8.3.27.1606
  bin1cv8: "/opt/1cv8/x86_64/8.3.27.1606/1cv8"
  binIbcmd: "/opt/1cv8/x86_64/8.3.27.1606/ibcmd" 
  rac: "/opt/1cv8/x86_64/8.3.27.1606/rac"
  
  # EDT версии 2024.2.6
  edtCli: "/opt/1C/1CE/components/1c-edt-2024.2.6+7-x86_64/1cedtcli"
```

### Windows

Типичные пути на Windows:

```yaml
paths:
  bin1cv8: "C:\\Program Files\\1cv8\\8.3.27.1606\\bin\\1cv8.exe"
  binIbcmd: "C:\\Program Files\\1cv8\\8.3.27.1606\\bin\\ibcmd.exe"
  rac: "C:\\Program Files\\1cv8\\8.3.27.1606\\bin\\rac.exe"
  edtCli: "C:\\Program Files\\1C\\1CE\\components\\1c-edt-2024.2.6+7\\1cedtcli.exe"
```

## Проверка конфигурации

### Валидация файлов конфигурации

Создайте скрипт проверки конфигурации:

```bash
#!/bin/bash
# validate-config.sh

echo "Проверка конфигурационных файлов..."

# Проверка наличия файлов
for file in app.yaml project.yaml dbconfig.yaml; do
    if [ ! -f "$file" ]; then
        echo "❌ Отсутствует файл: $file"
        exit 1
    else
        echo "✅ Найден файл: $file"
    fi
done

# Проверка YAML синтаксиса
for file in *.yaml; do
    if ! python3 -c "import yaml; yaml.safe_load(open('$file'))" 2>/dev/null; then
        echo "❌ Ошибка синтаксиса в файле: $file"
        exit 1
    else
        echo "✅ Синтаксис корректен: $file"
    fi
done

# Проверка путей к исполняемым файлам
echo "Проверка исполняемых файлов 1С..."
for path in $(yq eval '.paths[]' app.yaml); do
    if [ -x "$path" ]; then
        echo "✅ Найден исполняемый файл: $path"
    else
        echo "❌ Не найден или не исполняемый: $path"
    fi
done

echo "Проверка завершена!"
```

### Тестирование подключений

```bash
#!/bin/bash
# test-connections.sh

echo "Тестирование подключений..."

# Тест RAC подключения
if rac cluster list --server="$RAC_SERVER" --port="$RAC_PORT" >/dev/null 2>&1; then
    echo "✅ RAC подключение успешно"
else
    echo "❌ Ошибка подключения к RAC"
fi

# Тест Gitea API
if curl -s -H "Authorization: token $BR_ACCESS_TOKEN" \
   "$GITEA_URL/api/v1/user" >/dev/null; then
    echo "✅ Gitea API доступен"
else
    echo "❌ Ошибка подключения к Gitea API"
fi
```

## Автоматизация настройки

### Скрипт инициализации

```bash
#!/bin/bash
# init-benadis.sh - Скрипт инициализации среды apk-ci

set -e

echo "🚀 Инициализация apk-ci..."

# Создание рабочих директорий
echo "📁 Создание рабочих директорий..."
mkdir -p /tmp/benadis/{temp,logs,backup}
mkdir -p /opt/benadis/{config,scripts}

# Копирование шаблонов конфигурации
echo "📄 Копирование шаблонов конфигурации..."
cp templates/app.yaml.template app.yaml
cp templates/project.yaml.template project.yaml
cp templates/dbconfig.yaml.template dbconfig.yaml

# Настройка переменных окружения
echo "🔧 Настройка переменных окружения..."
cat >> ~/.bashrc << 'EOF'
# apk-ci environment variables
export BR_WORK_DIR="/tmp/benadis"
export BR_TMP_DIR="/tmp/benadis/temp"
export BR_LOG_LEVEL="Info"

# 1C Platform paths
export Bin1cv8="/opt/1cv8/x86_64/8.3.27.1606/1cv8"
export BinIbcmd="/opt/1cv8/x86_64/8.3.27.1606/ibcmd"
export RacPath="/opt/1cv8/x86_64/8.3.27.1606/rac"
EOF

# Проверка установки 1С
echo "🔍 Проверка установки платформы 1С..."
if [ -x "/opt/1cv8/x86_64/8.3.27.1606/1cv8" ]; then
    echo "✅ Платформа 1С найдена"
else
    echo "❌ Платформа 1С не найдена. Установите 1С:Предприятие."
    exit 1
fi

echo "✅ Инициализация завершена!"
echo "💡 Отредактируйте конфигурационные файлы под вашу среду."
```

### Docker Compose для тестовой среды

```yaml
# docker-compose.test.yml
version: '3.8'

services:
  benadis-test:
    build: .
    container_name: apk-ci-test
    environment:
      - BR_LOG_LEVEL=Debug
      - BR_WORK_DIR=/opt/benadis
      - RAC_SERVER=1c-server
      - RAC_PORT=1545
    volumes:
      - ./config:/opt/benadis/config
      - ./logs:/opt/benadis/logs
      - /opt/1cv8:/opt/1cv8:ro
    depends_on:
      - gitea
      - mssql
      
  gitea:
    image: gitea/gitea:latest
    container_name: gitea-test
    environment:
      - GITEA__database__DB_TYPE=sqlite3
    ports:
      - "3000:3000"
    volumes:
      - gitea-data:/data
      
  mssql:
    image: mcr.microsoft.com/mssql/server:2019-latest
    container_name: mssql-test
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=TestPassword123!
    ports:
      - "1433:1433"
    volumes:
      - mssql-data:/var/opt/mssql

volumes:
  gitea-data:
  mssql-data:
```

## Troubleshooting

### Распространенные проблемы конфигурации

#### "Не удалось загрузить конфигурацию приложения"

**Причины:**
- Отсутствует файл app.yaml
- Ошибка синтаксиса YAML
- Неверные права доступа

**Решение:**
```bash
# Проверка наличия файла
ls -la app.yaml

# Проверка синтаксиса YAML
python3 -c "import yaml; print(yaml.safe_load(open('app.yaml')))"

# Исправление прав доступа
chmod 644 app.yaml
```

#### "Не найден исполняемый файл 1cv8"

**Причины:**
- Неверный путь в конфигурации
- Платформа 1С не установлена
- Неверные права доступа

**Решение:**
```bash
# Поиск установленной платформы
find /opt -name "1cv8" -type f 2>/dev/null

# Проверка прав выполнения
ls -la /opt/1cv8/x86_64/*/1cv8

# Обновление пути в app.yaml
yq eval '.paths.bin1cv8 = "/correct/path/to/1cv8"' -i app.yaml
```

#### "Ошибка подключения к RAC"

**Причины:**
- Неверные параметры сервера
- Сервер 1С недоступен
- Проблемы с сетью

**Решение:**
```bash
# Проверка доступности сервера
ping $RAC_SERVER

# Проверка открытого порта
telnet $RAC_SERVER $RAC_PORT

# Тест RAC подключения
rac cluster list --server=$RAC_SERVER --port=$RAC_PORT
```

### Логи и диагностика

#### Включение отладочного режима
```bash
export BR_LOG_LEVEL="Debug"
```

#### Анализ логов
```bash
# Просмотр последних логов
tail -f /tmp/benadis/logs/apk-ci.log

# Поиск ошибок
grep -i error /tmp/benadis/logs/apk-ci.log

# Фильтрация по компоненту
grep "rac\|RAC" /tmp/benadis/logs/apk-ci.log
```

---

*Версия документа: 1.0*  
*Последнее обновление: 2025-08-26*