// Тестовый BSL файл с проблемами для проверки исправлений
// Этот файл содержит различные проблемы, которые могут вызвать ошибки токенизации

Процедура ТестоваяПроцедура()
	// Строка с незакрытой кавычкой
	Сообщение = "Это строка без закрывающей кавычки;"

	// Строка с неразрывными пробелами (U+00A0)
	ТекстСНеразрывнымиПробелами = "Текст с неразрывными пробелами";

	// Смешанные окончания строк (будут добавлены при записи)
	Переменная1 = "Значение1";
	Переменная2 = "Значение2";

	// Строка с лишними пробелами в конце
	СтрокаСПробелами = "Значение";

КонецПроцедуры

функция ТестоваяФункция()
	// Неправильный регистр ключевого слова
	Возврат "Результат";
КонецФункции

// Процедура с проблемными символами
Процедура ПроцедураСПроблемами()
	// Строка с недопустимыми символами
	НедопустимыеСимволы = "Текст с символами: ";

	// Комментарий с проблемным символом перед //
	Переменная = "Значение"; // Проблемный комментарий

КонецПроцедуры
