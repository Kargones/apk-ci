# apk-ci

Инструмент для автоматизации работы с системой 1С:Предприятие, включающий модули для конвертации данных, восстановления баз данных, управления сервисным режимом и работы с EDT.

## Возможности

- **Convert Module**: Конвертация данных между различными форматами
- **DBRestore Module**: Восстановление и управление базами данных MSSQL
- **ServiceMode Module**: Управление сервисным режимом информационных баз 1С
- **EDT Module**: Интеграция с 1C:Enterprise Development Tools
- **Централизованная система конфигурации**: Единый подход к настройке всех модулей

## Установка

### Из исходного кода

```bash
git clone <repository-url>
cd apk-ci
go build -o apk-ci ./cmd/apk-ci
```

### Использование в качестве библиотеки

```bash
go get github.com/your-org/apk-ci
```

## Конфигурация

### Централизованная система конфигурации

Все модули используют единую систему конфигурации, которая поддерживает:

1. **Переменные окружения** (наивысший приоритет)
2. **Конфигурационные файлы** (JSON/YAML)
3. **Значения по умолчанию** (наименьший приоритет)

### Переменные окружения

Каждый модуль использует свой префикс:

- **Convert**: `CONVERT_`
- **DBRestore**: `DBRESTORE_`
- **ServiceMode**: `SERVICE_MODE_`
- **EDT**: `EDT_`

### Пример конфигурации

Скопируйте файл примера конфигурации:

```bash
cp examples/all-modules-config.env .env
```

Отредактируйте `.env` файл под вашу среду.

## Использование

### Командная строка

```bash
# Управление сервисным режимом
./apk-ci service-mode-enable --infobase MyInfobase

# Восстановление базы данных
./apk-ci dbrestore --config config.yaml

# Конвертация данных
./apk-ci convert --config convert-config.json
```

### Программный интерфейс

```go
package main

import (
    "github.com/Kargones/apk-ci/internal/servicemode"
    "github.com/Kargones/apk-ci/internal/entity/dbrestore"
)

func main() {
    // Загрузка конфигурации сервисного режима
    config, err := servicemode.LoadServiceModeConfig()
    if err != nil {
        log.Fatal(err)
    }
    
    // Создание клиента
    client := servicemode.NewClient(config, logger)
    
    // Использование клиента
    err = client.EnableServiceMode(ctx, "MyInfobase", false)
}
```

## Модули

### Convert Module

Модуль для конвертации данных между различными форматами.

**Конфигурация:**
- Файл: JSON формат
- Переменные окружения: `CONVERT_*`
- Пример: `examples/convert-config-example.go`

### DBRestore Module

Модуль для восстановления и управления базами данных MSSQL.

**Конфигурация:**
- Файл: YAML формат
- Переменные окружения: `DBRESTORE_*`
- Пример: `examples/dbrestore-config-example.go`

### ServiceMode Module

Модуль для управления сервисным режимом информационных баз 1С.

**Конфигурация:**
- Переменные окружения: `SERVICE_MODE_*`
- Пример: `examples/servicemode-config-example.go`

### EDT Module

Модуль для интеграции с 1C:Enterprise Development Tools.

**Конфигурация:**
- Файл: JSON формат
- Переменные окружения: `EDT_*`
- Пример: `examples/edt-config-example.go`

## Примеры

В директории `examples/` находятся:

- Примеры кода для каждого модуля
- Конфигурационные файлы
- Демонстрационные скрипты
- Подробная документация по использованию

## Обратная совместимость

Все модули сохраняют полную обратную совместимость со старыми методами конфигурации. Новые функции добавлены параллельно существующим.

## Тестирование

```bash
# Запуск всех тестов
go test ./...

# Запуск тестов конфигурации
go test ./internal/config

# Запуск тестов с покрытием
go test -cover ./...
```

## Документация

### 📚 Полная документация WIKI
**[➤ Перейти к документации WIKI](docs/wiki/README.md)**

В WIKI доступна исчерпывающая документация:
- [Обзор проекта](docs/wiki/00-Обзор-проекта.md) — введение и архитектура
- [Справочник команд CLI](docs/wiki/01-Справочник-команд.md) — полное описание всех команд
- [Интеграция с 1cv8](docs/wiki/02-Интеграция-1cv8.md) — работа с платформой 1С
- [Архитектура модулей](docs/wiki/03-Архитектура-модулей.md) — техническая архитектура
- [Руководство по конфигурации](docs/wiki/04-Конфигурация.md) — настройка и конфигурирование
- [API документация](docs/wiki/05-API-документация.md) — программный интерфейс
- [Разработка и тестирование](docs/wiki/06-Разработка-и-тестирование.md) — для разработчиков

### 📖 Дополнительные документы
- `docs/architecture.md` - Техническая архитектура проекта
- `docs/service-mode-usage.md` - Детальное использование сервисного режима
- `examples/README.md` - Примеры использования

## Лицензия

[Укажите лицензию]

## Поддержка

Для получения помощи:
1. **📚 Изучите WIKI**: [Полная документация](docs/wiki/README.md) с подробными руководствами
2. **🔍 Посмотрите примеры**: [Справочник команд](docs/wiki/01-Справочник-команд.md) с примерами сценариев
3. **⚙️ Проверьте конфигурацию**: [Руководство по конфигурации](docs/wiki/04-Конфигурация.md)
4. **📝 Создайте issue**: Опишите проблему в репозитории проекта
  push:
    branches-ignore: [testMerge]
  pull_request_target:
    types:
      - opened
      - reopened
    branches-ignore: [testMerge]
jobs:
  Check-merge-conflict:
    runs-on: debian-latest
    container:
      image: golang:1.21
    steps:
      - name: Проверка конфликтов слияния
        id: megre-test
        uses: https://${{ secrets.READER_TOKEN }}:@git.apkholding.ru/actions/merge-test@latest
        with:
          giteaURL: ${{ github.server_url }}
          repository: ${{ github.repository }}
          accessToken: ${{ secrets.TOKEN_FULL }}
```

- Сохранить файл (сделать коммит).

## Использование

При создании запроса на слияние или внесении изменений в основную ветку репозитория будет автоматически запускаться процесс проверки конфликтов слияния для всех открытых запросов на слияние в основную ветку.

При выявлении конфликта, вызывающий конфликт запрос на слияние автоматически закрывается.
