# Epic 1: Architectural Foundation

**Статус:** 🟡 В планировании
**Приоритет:** Критический (блокирует все последующие)
**Риск:** 🟡 Средний
**Stories:** 9
**FRs:** FR1-5, FR29-35, FR41-43, FR47-50

---

## Цель

Создать SOLID-совместимую архитектурную основу с DI-контейнером, Command Registry, структурированным выводом и базовым observability. После завершения — первая NR-команда работает в production.

## Ценность

Wire DI + Command Registry + Output + Logging + Tracing + первая NR-команда

---

## Волны выполнения (оптимальный параллелизм)

```
ВОЛНА 1 (параллельно):     1.1 Registry | 1.3 Output+Errors | 1.4 Logger | 1.6 Config
                                │                │                │
ВОЛНА 2:                        │                │                │
                           1.2 Deprecated   1.5 TraceID ─────────┘
                                │                │
ВОЛНА 3:                        └────────────────┴──────► 1.7 Wire DI
                                                              │
ВОЛНА 4:                                                 1.8 nr-version
                                                              │
ВОЛНА 5:                                                 1.9 Help
```

**Критический путь:** 1.1 → 1.2 → 1.7 → 1.8 → 1.9 (5 историй)

---

## Decision Matrix

| Story | Приоритет | Score | Ключевой фактор |
|-------|-----------|-------|-----------------|
| 1.1 Registry | 🥇 1 | 38 | Максимальная блокировка (5 историй зависят) |
| 1.3 Output | 🥈 2 | 37 | Критично для демонстрации |
| 1.4 Logger | 🥉 3 | 31 | Observability с первого дня |
| 1.7 Wire DI | ⚠️ | 36 | Высокий риск (4/5), много зависимостей |
| 1.9 Help | 9 | 12 | Nice-to-have, можно отложить |

---

## Pre-mortem: Ключевые риски

| ID | Failure Mode | Вероятность | Влияние | Митигация в AC |
|----|--------------|-------------|---------|----------------|
| FM1 | Wire DI не компилируется | Средняя | Среднее | Unit-тесты providers, `go generate` в CI |
| FM2 | Registry конфликт с main.go | Средняя | Высокое | Fallback логика, логирование пути |
| FM3 | JSON breaking changes | Высокая | Среднее | Golden tests, JSON Schema, API version |
| FM4 | Логи смешиваются с output | Высокая | Высокое | Logger→stderr, запрет fmt.Print* |
| FM5 | CI build отличается от local | Низкая | Среднее | CI тест бинарника, fallback build info |
| FM6 | Config ломает старые файлы | Средняя | Среднее | Optional поля, backward compat тест |

---

## Stories

### Story 1.1: Command Registry с self-registration

**Приоритет:** P0 | **Размер:** M | **Риск:** Medium

**As a** разработчик,
**I want** регистрировать новые команды через init() без изменения main.go,
**So that** добавление команд соответствует Open/Closed Principle.

**Acceptance Criteria:**

- [ ] Handler вызывает `command.Register()` в init() → команда доступна через `command.Get(name)`
- [ ] Registry возвращает `(nil, false)` для несуществующих команд
- [ ] Повторная регистрация с тем же именем паникует (programming error)
- [ ] main.go: сначала проверяет Registry, потом fallback на legacy switch
- [ ] Логируется какой путь выбран ("registry" или "legacy") для диагностики

**Technical Notes:**
- Файлы: `internal/command/registry.go`, `internal/command/handler.go`
- Handler interface: `Name() string`, `Execute(ctx, cfg) error`
- Thread-safe не требуется (регистрация только в init)
- ⚠️ Pre-mortem FM2: Интеграция с main.go требует fallback логики

---

### Story 1.2: NR-Migration Bridge (DeprecatedBridge)

**Приоритет:** P0 | **Размер:** S | **Риск:** Low
**Prerequisites:** Story 1.1

**As a** DevOps-инженер,
**I want** получать warning при использовании deprecated команд,
**So that** я знаю о необходимости миграции на NR-версии.

**Acceptance Criteria:**

- [ ] Команда зарегистрирована через `RegisterWithAlias(handler, "old-name")` → warning в stderr
- [ ] Warning содержит: старое имя, новое имя, текст "deprecated"
- [ ] Команда выполняется через actual handler
- [ ] Warning выводится на каждый вызов (не кэшируется)

**Technical Notes:**
- Файл: `internal/command/deprecated.go`
- DeprecatedBridge struct: actual Handler, deprecated string, newName string
- Warning через log.Warn (после Story 1.5) или fmt.Fprintf(os.Stderr) временно

---

### Story 1.3: OutputWriter + Structured Errors

**Приоритет:** P0 | **Размер:** L | **Риск:** Medium

**As a** DevOps-инженер,
**I want** получать результаты команд в JSON формате со структурированными ошибками,
**So that** я могу интегрировать benadis-runner с другими инструментами автоматизации.

**Acceptance Criteria:**

- [ ] BR_OUTPUT_FORMAT=json → результат в JSON:
  ```json
  {
    "status": "success",
    "command": "command-name",
    "data": { ... },
    "metadata": { "duration_ms": 123, "api_version": "v1" }
  }
  ```
- [ ] BR_OUTPUT_FORMAT=text → человекочитаемый формат (по умолчанию)
- [ ] Ошибки: `"error": {"code": "...", "message": "..."}`
- [ ] Логи НЕ смешиваются с результатом (логи → stderr)
- [ ] AppError реализует error interface + Unwrap()
- [ ] Секреты НИКОГДА не попадают в Message
- [ ] Golden file тесты для стабильности JSON формата
- [ ] JSON Schema определена для Result struct

**Technical Notes:**
- Файлы: `internal/pkg/output/writer.go`, `text.go`, `json.go`, `internal/pkg/errors/errors.go`
- Коды ошибок: ERR_CONFIG_LOAD, ERR_DB_CONNECTION, ERR_SERVICE_MODE, etc.
- YAML formatter отложен до Epic 7
- ⚠️ Pre-mortem FM3: Golden tests предотвращают breaking changes

---

### Story 1.4: Logger interface + slog adapter

**Приоритет:** P0 | **Размер:** M | **Риск:** Low

**As a** DevOps-инженер,
**I want** получать структурированные JSON логи,
**So that** я могу анализировать их автоматизированными инструментами.

**Acceptance Criteria:**

- [ ] logging.format=json → запись содержит: timestamp, level, msg, атрибуты
- [ ] Уровни: DEBUG, INFO, WARN, ERROR
- [ ] Уровень настраивается через конфигурацию
- [ ] `Logger.With(args)` возвращает новый logger с добавленными атрибутами
- [ ] Logger пишет ТОЛЬКО в stderr (никогда в stdout)
- [ ] golangci-lint правило запрещает fmt.Print* в production коде

**Technical Notes:**
- Файлы: `internal/pkg/logging/logger.go`, `slog.go`
- Logger interface абстрагирует slog для тестирования
- File rotation отложена до Epic 6
- ⚠️ Pre-mortem FM4: Логи в stdout ломают JSON парсеры downstream

---

### Story 1.5: Trace ID generation

**Приоритет:** P1 | **Размер:** S | **Риск:** Low
**Prerequisites:** Story 1.4

**As a** DevOps-инженер,
**I want** иметь trace_id в каждой записи лога,
**So that** я могу коррелировать логи одной операции.

**Acceptance Criteria:**

- [ ] trace_id имеет формат UUID v4 или 16-byte hex string
- [ ] trace_id добавляется в context
- [ ] Все последующие логи содержат trace_id
- [ ] trace_id включается в JSON output metadata

**Technical Notes:**
- Файл: `internal/pkg/tracing/traceid.go`
- Использовать crypto/rand для генерации
- OpenTelemetry export отложен до Epic 6
- Context key: type traceIDKey struct{}

---

### Story 1.6: Config extensions (implementations, logging sections)

**Приоритет:** P0 | **Размер:** M | **Риск:** Medium

**As a** DevOps-инженер,
**I want** настраивать выбор реализаций через конфигурацию,
**So that** я могу переключаться между 1cv8/ibcmd без изменения кода.

**Acceptance Criteria:**

- [ ] Секция implementations: config_export, db_create
- [ ] Секция logging: format, level, file (опционально)
- [ ] Defaults: config_export="1cv8", format="text", level="info"
- [ ] Переменные окружения переопределяют файл конфигурации
- [ ] ВСЕ новые поля optional с разумными defaults
- [ ] Тест: существующий production конфиг парсится без ошибок

**Technical Notes:**
- Файл: `internal/config/config.go` (расширение)
- Новые struct: ImplementationsConfig, LoggingConfig
- ⚠️ Pre-mortem FM6: Required поля без defaults ломают старые конфиги

---

### Story 1.7: Wire DI setup + providers

**Приоритет:** P0 | **Размер:** L | **Риск:** High
**Prerequisites:** Story 1.1, 1.3, 1.4

**As a** разработчик,
**I want** получать зависимости через Wire injection,
**So that** код соответствует Dependency Inversion Principle.

**Acceptance Criteria:**

- [ ] Wire definitions в internal/di/wire.go
- [ ] `go generate ./internal/di/...` генерирует wire_gen.go
- [ ] Провайдеры для: Config, Logger, OutputWriter
- [ ] Ошибки зависимостей обнаруживаются на этапе компиляции
- [ ] Циклические зависимости вызывают ошибку компиляции
- [ ] Unit-тест для каждого provider (not nil)
- [ ] `go generate ./internal/di/...` выполняется в CI pipeline
- [ ] Интерфейсы в отдельных файлах `interfaces.go`

**Technical Notes:**
- Файлы: `internal/di/wire.go`, `providers.go`, `wire_gen.go` (generated)
- Wire v0.6.0
- ⚠️ Pre-mortem FM1: Wire — высокий риск, требует тщательного тестирования

---

### Story 1.8: First NR-command: nr-version

**Приоритет:** P0 | **Размер:** M | **Риск:** Low
**Prerequisites:** Story 1.1-1.7

**As a** DevOps-инженер,
**I want** выполнить nr-version команду,
**So that** я могу проверить что новая архитектура работает.

**Acceptance Criteria:**

- [ ] BR_COMMAND=nr-version → версия приложения, Go версия, дата сборки
- [ ] Вывод соответствует OutputWriter формату (text/JSON)
- [ ] Команда зарегистрирована через Registry
- [ ] trace_id присутствует в логах
- [ ] exit code = 0 при успехе
- [ ] Fallback: version="dev", date="unknown" если build info недоступен
- [ ] CI job тестирует собранный бинарник (не `go run`)
- [ ] stdout содержит ТОЛЬКО Result JSON
- [ ] Интеграционный тест: Registry + legacy switch работают вместе

**Technical Notes:**
- Файл: `internal/command/handlers/version/version.go`
- Build info через runtime/debug.ReadBuildInfo() или ldflags
- Makefile target: `build-with-version` с ldflags
- ⚠️ Pre-mortem FM5: `go run` работает, собранный бинарник — нет

---

### Story 1.9: Auto-generated help from Registry

**Приоритет:** P2 | **Размер:** S | **Риск:** Low
**Prerequisites:** Story 1.1, 1.2, 1.8

**As a** DevOps-инженер,
**I want** видеть список всех доступных команд,
**So that** я знаю какие команды существуют без чтения документации.

**Acceptance Criteria:**

- [ ] BR_COMMAND=help или пустой → список всех команд с описаниями
- [ ] Deprecated команды помечены как [deprecated]
- [ ] NR-команды группируются отдельно от legacy
- [ ] Вывод поддерживает text/JSON формат

**Technical Notes:**
- Файл: `internal/command/help.go`
- Handler interface расширить: Description() string (опционально)
- Группировка: по категориям или алфавиту

---

## Six Thinking Hats Summary

| Hat | Insight | Действие |
|-----|---------|----------|
| ⚪ White | Не хватает данных о downstream JSON парсерах | Собрать production конфиги для тестов |
| 🔴 Red | Wire вызывает беспокойство (новая технология) | Внутренняя документация с примерами |
| ⚫ Black | Registry может игнорироваться разработчиками | CI check: новые команды через Registry |
| 🟡 Yellow | После Epic 1 — добавление команды = 1 файл | Коммуницировать vision команде |
| 🟢 Green | nr-health полезнее nr-version для production | Рассмотреть как Story 1.8b (опционально) |
| 🔵 Blue | Нужен процесс контроля качества | Code review checklist для архитектуры |

---

## Чеклист перед началом

- [ ] Собрать примеры production конфигов для backward compatibility тестов
- [ ] Создать Wiki страницу по Wire с примерами из проекта
- [ ] Настроить CI check: файлы в `internal/command/handlers/` должны вызывать `command.Register()`

---

## Definition of Done

- [ ] nr-version работает в production pipeline
- [ ] Все тесты проходят
- [ ] Документация Wire обновлена
- [ ] CI pipeline обновлён (`go generate`, golden tests)

---

## Связанные документы

- [Epic Overview](./index.md)
- [FR Coverage](./fr-coverage.md)
- Architecture ADR-001 (Wire DI)
- Architecture ADR-002 (Command Registry)
- Architecture ADR-005 (Logging)

---

_Последнее обновление: 2025-11-25_
